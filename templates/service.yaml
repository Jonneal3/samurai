apiVersion: v1
kind: Service
metadata:
  name: {{ include "samurai.fullname" . }}
  labels:
    {{- include "samurai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "samurai.selectorLabels" . | nindent 4 }}
